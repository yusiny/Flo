<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.flo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Flotemplate">
        <activity android:name=".SplashActivity">
        <!--splash activity에 현재 상태
        //activity로 intent-filter 파일이 옮겨가며 화면 전환 -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>

        <activity android:name="com.example.flo.MainActivity" />
        <activity android:name="com.example.flo.SongActivity" />
        <activity android:name="com.example.flo.LoginActivity" />
        <activity android:name="com.example.flo.SignupAcitivity" />
    </application>

</manifest>